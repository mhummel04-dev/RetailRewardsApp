<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RetailRewardsApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:RetailRewardsApp.Mobile.Views"
    Title="RetailRewardsApp">


    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="login"
    />

    <TabBar Route="main">

        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:HomePage}"
        />

        <ShellContent
            Title="Scan"
            ContentTemplate="{DataTemplate local:ScanPage}"
        />

        <ShellContent
            Title="Menu"
            ContentTemplate="{DataTemplate local:MenuPage}"
        />

        <ShellContent
            Title="Offers"
            ContentTemplate="{DataTemplate local:OffersPage}"
        />

        <ShellContent
            Title="Profile"
            ContentTemplate="{DataTemplate local:ProfilePage}"
        />
       
    </TabBar>

</Shell>
